/**
 *
 * @authors Your Name (you@example.org)
 * @date    2017-08-07 15:24:20
 * @version $Id$
 */
import axios from 'axios'

/*
* xx管理
*/
// get
function get (param) {
  return axios.get('/dologin',{params:{
    'name'        : param['name'],
    'name'        : param['name'],
    'name'        : param['name'],
    'name'        : param['name'],
    'name'        : param['name'],
    'name'        : param['name'],
    'name'        : param['name'],
    'name'        : param['name']
  }})
}

// post
function xxx (param) {
  return axios.post('xxx',
    {
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name']
    })
}
// xxx
function xxx (param) {
  return axios.post('xxx',
    {
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name']
    })
}
// xxx
function xxx (param) {
  return axios.post('xxx',
    {
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name']
    })
}
// xxx
function xxx (param) {
  return axios.post('xxx',
    {
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name']
    })
}
// xxx
function xxx (param) {
  return axios.post('xxx',
    {
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name']
    })
}
// xxx
function xxx (param) {
  return axios.post('xxx',
    {
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name'],
      'name'        : param['name']
    })
}
// xxx
function xxx (param) {
  return axios.post('xxx',
    {
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name
    })
}

// xxx
function xxx (param) {
  return axios.post('xxx',
    {
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name
    })
}

// xxx
function xxx (param) {
  return axios.post('xxx',
    {
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name
    })
}

// xxx
function xxx (param) {
  return axios.post('xxx',
    {
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name,
      'name'        : param.name
    })
}


export default {
  xxx,
  xxx,
  xxx,
  xxx,
  xxx,
  xxx,
  xxx,
  xxx,
  xxx,
  xxx,
  xxx,
  xxx
}

